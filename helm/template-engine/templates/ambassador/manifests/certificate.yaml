##
{{- if .Values.ambassador }}
{{- if and .Values.global.manifests (hasKey .Values.global "manifests")}}
{{- if .Values.ambassador.dynamics.k8ssso.certificate }}
{{- if and .Values.ambassador.dynamics.k8ssso.certificate.useCa (hasKey .Values.ambassador.dynamics.k8ssso.certificate "useCa")}}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: cluster-issuer-ca.{{ .Values.ambassador.dynamics.k8ssso.fqdn}}
  namespace: {{ template "common.names.namespace" . }}
  labels: {{- include "common.labels.matchLabels" . | nindent 4 }}
    app.kubernetes.io/component: dependency-injection
    app.kubernetes.io/part-of: datawire.edge-stack
    app.kubernetes.io/managed-by: rdbox
    rdbox.local/format: v1beta1
    rdbox.local/chart: {{ .Chart.Version }}
spec:
  isCA: true
  commonName: "{{ .Values.ambassador.dynamics.k8ssso.fqdn}}"
  dnsNames:
    - {{ .Values.ambassador.dynamics.k8ssso.fqdn}}
    - "*.{{ .Values.ambassador.dynamics.k8ssso.fqdn}}"
  secretName: {{ .Values.ambassador.dynamics.k8ssso.fqdn}}
  duration: {{ .Values.ambassador.di.certificate.duration }}
  {{- if .Values.ambassador.di.certificate.privateKey }}
  privateKey: {{- include "common.tplvalues.render" ( dict "value" .Values.ambassador.di.certificate.privateKey "context" $) | nindent 4 }}
  {{- end }}
  {{- if .Values.ambassador.di.certificate.subject }}
  subject: {{- include "common.tplvalues.render" ( dict "value" .Values.ambassador.di.certificate.subject "context" $) | nindent 4 }}
  {{- end }}
  issuerRef:
    name: cluster-issuer-ca.{{ .Values.ambassador.dynamics.common.baseFqdn }}
    kind: ClusterIssuer
    group: cert-manager.io
{{- end }}
{{- end }}
{{- end }}
{{- end }}